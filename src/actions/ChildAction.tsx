import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const ChildAction = () => {
  return (
    <View>
      <Text>ChildAction</Text>
    </View>
  );
};

export default ChildAction;

const styles = StyleSheet.create({});
