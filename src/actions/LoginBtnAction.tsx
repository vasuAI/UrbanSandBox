import ActionType from './ActionType';
import CommonFunction from '../utils/CommonFunction';

/**
 * @description handing user login flow
 * @param email
 * @param password
 * @returns
 */
const loginBtnAction = (email: any, password: any) => {
  return (dispatch: Function) => {
    // calling an api
  };
};
export default loginBtnAction;
